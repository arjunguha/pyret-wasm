A session-scoped Stop hook is now active with condition: "This is an empty repository. In ./pyret, I have checked out the code for the Pyret programming language. If you need them, the Pyret docs are at https://github.com/brownplt/pyret-docs.

In this repository, I want you to build a new WebAssembly based compiler and runtime system for Pyret. Match the semantics of Pyret exactly, including numbers, tail calls, etc. It should be possible to run both on the code (bun is installed) and on the web. On the web, Pyret has frontend features that cannot be done in WebAssembly and need to be done in JavaScript. So, you can do that, but ensure the JavaScript code is minimal. You should get a web-based IDE working that implements all of https://github.com/brownplt/code.pyret.org as well using this new compiler and runtime. 

It should go without saying that the error messages that the Pyret team is so proud of, and the stop button, should both work.

Good luck bro.". Briefly acknowledge the goal, then immediately start (or continue) working toward it — treat the condition itself as your directive and do not pause to ask the user what to do. The hook will block stopping until the condition holds. It auto-clears once the condition is met — do not tell the user to run `/goal clear` after success; that's only for clearing a goal early.